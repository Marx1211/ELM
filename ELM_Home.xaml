<Window x:Class="ELM.ELM_Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ELM"
      mc:Ignorable="d" 
      Height="350" Width="600"
      Title="ELM - Home">
    
    <Grid Margin="10,0,10,10" Background="WhiteSmoke">

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" VerticalAlignment="Center" FontFamily="Trebuchet MS" 
        FontWeight="Bold" FontSize="36">
            Euston Leisure Messaging
        </Label>

        
        <Button Name="manual_btn" Grid.Column="1" Grid.Row="3" Margin="0,10,0,0" Width="125" Height="50" HorizontalAlignment="Right" Click="manual_btn_Click">Manual input</Button>
        <Button Name="loadFile_btn" Grid.Column="1" Grid.Row="4" Margin="0,10,0,0" Width="125" Height="50" HorizontalAlignment="Right" Click="loadFile_btn_Click">Load from file</Button>
    </Grid>
</Window>
